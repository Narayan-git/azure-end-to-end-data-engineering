# Power BI Dashboard Guide

1. Load warehouse data (Azure SQL) into Power BI.
2. Connect using server details, run sample SQL: SELECT DiagnosisCode, COUNT(*) FROM Admissions GROUP BY DiagnosisCode
3. Build charts: Admissions trend, cost per admission, diagnosis prevalence

Sample PBIX: /PowerBI/healthcare_demo.pbix

Focus: Visual clarity, actionable insights for hospital admins.